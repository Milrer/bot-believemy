// LES IMPORTS

// ex : const Discord = require ('discord.js');

module.exports.run = (client, message, args) => {

	// LE CODE
	// ex : message.channel.send('coucou')

}

module.exports.help = {
    name: "", // LE NOM DE LA COMMANDE
    // ex : commande
    aliases: [""] // LES ALTERNATIVE
    // ex : cmd
}